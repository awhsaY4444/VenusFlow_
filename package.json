{
  "name": "venusflow",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client && npm run build --workspace server && npx shx mkdir -p server/dist && npx shx cp -r client/dist/* server/dist/",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
